<%# The active/deleted state of one form, said in words rather than by colour alone. %>
<% if form.deleted? %>
  <span class="badge badge-deleted">Deleted</span>
<% else %>
  <span class="badge badge-admin">Active</span>
<% end %>
