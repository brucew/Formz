<div class="card px-6 py-12 text-center">
  <h2 class="text-base font-semibold text-slate-900"><%= title %></h2>
  <p class="mx-auto mt-2 max-w-md prose-muted"><%= description %></p>
  <% if defined?(yield) %>
    <div class="mt-6 flex justify-center gap-3"><%= yield %></div>
  <% end %>
</div>
